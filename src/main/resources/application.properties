spring.application.name=ifinder

spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}


spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ifinder.istad@gmail.com
spring.mail.password=obhw rmoj azxm njtq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#media configuration
media.server-path=${MEDIA_SERVER_PATH}
media.client-path=/media/**
media.base-uri=${MEDIA_BASE_URI}

#TypeSense configuration
typesense.ip=${TYPESENSE_IP}
typesense.port=${TYPESENSE_PORT}
typesense.apiKey=${TYPESENSE_API_KEY}


#file upload configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

keys.access-private-token=keys/access-private
keys.access-public-token=keys/access-public
keys.refresh-public-token=keys/refresh-public
keys.refresh-private-token=keys/refresh-private

#select profile
spring.profiles.active=prod

#swaggui
springdoc.swagger-ui.path=/doc
springdoc.api-docs.path=/api-docs

#google custom search
google.custom-search.api-key=${GOOGLE_CUSTOM_SEARCH_API_KEY}y
google.custom-search.engine-id=${SEARCH_ENGINE_ID}
google.application-name=${GOOGLE_APPLICATION_NAME}

#gemini api key
gemini.api-key=${GEMINI_API_KEY}

#coud translation api key
cloud.translation.api-key=${CLOUD_TRANSLATION_API_KEY}

#scrape express js url
scrape.express-js-url=${SCRAPE_EXPRESS_JS_URL}

server.port=${SERVER_PORT}

#json file directory
json.file.directory=${JSON_FILE_DIRECTORY}

